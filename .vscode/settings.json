{
	// GO
	"[go]": {
		"editor.insertSpaces": false
	},

	// Editor
	// Should ISense accept highlighted suggestion on Enter
	"editor.acceptSuggestionOnEnter": "on",

	// Autoclosing
	"editor.autoClosingBrackets": "always",
	"editor.autoClosingDelete": "always",
	"editor.autoClosingQuotes": "always",

	// Should find function automatically find user-input in search field
	"editor.find.autoFindInSelection": "never",

	// Separate code(block) from search results with 1 newline
	"editor.find.addExtraSpaceOnTop": false,

	// Cursor style
	"editor.cursorStyle": "line",

	// Autoformat code on save action
	"editor.formatOnSave": true,

	// Saving mode
	"editor.formatOnSaveMode": "file",

	// Formatting on code paste
	"editor.formatOnPaste": true,

	// MiniMap
	"editor.minimap.enabled": false,


	// Workbench
	// Expanding mode
	"workbench.tree.expandMode": "singleClick",

	// Indenting (subdirs) size 
	"workbench.tree.indent": 8,

	// Small dots as indent guides
	"workbench.tree.renderIndentGuides": "onHover",

	// VSCode theme
	"workbench.colorTheme": "GitHub Light",

	// Close editor on file delete
	"workbench.editor.closeOnFileDelete": true,

	// Position of closing X on tab
	"workbench.editor.tabCloseButton": "left",

	// Workbernch starts with this. Since workbench is loaded at startup, this is what is seen upon VSCode launch.
	"workbench.startupEditor": "none",
	

	// Files
	// Autosave strategy
	"files.autoSave": "afterDelay",

	// Delay from last change to autosave triggering
	"files.autoSaveDelay": 500, // miliseconds; 500ms = .5 second

	// Do not insert newline at file end.
	"files.insertFinalNewline": false,

	// Ask prior saving conflicting files (useful in conjunction with VCS)
	"files.saveConflictResolution": "askUser",


	// MarkDown
	// Files to ignore when linting. Ignoring own ruleset is by default.
	"markdownlint.ignore": ".markdownlintignore",

	// Diffs ( VCS )
	"[diff]": {

		// CodeLens (ext)
		"diffEditor.codeLens": true,

		// Diff engine type
		"diffEditor.diffAlgorithm": "smart",

		// Ignore / trim whitespace (EOF)
		"diffEditor.ignoreTrimWhitespace": true,

		// Max TTW for diff to generate (ms)
		"diffEditor.maxComputationTime": 5000,

		// Max diffs per file size (in MB)
		"diffEditor.maxFileSize": 50,

		// Indicator is either "+" for additions, or "-" for deletion
		"diffEditor.renderIndicators": true,

		// Is margin revertable? (redo sign)
		"diffEditor.renderMarginRevertIcon": true,

		// Render diffs left-right panes vertically
		"diffEditor.renderSideBySide": true,

		// Wrapping words in diff
		"diffEditor.wordWrap": "on"
	},

	// Navigate to next conflict when previous is resolved
	"merge-conflict.autoNavigateNextConflict.enabled": true,

	// CodeLens (ext)
	"merge-conflict.codeLens.enabled": true,

	// Are conflicts colored
	"merge-conflict.decorators.enabled": true,

	// View of diffs of conflicting file(s) position
	"merge-conflict.diffViewPosition": "Below",

	// MergeEditor
	// Algorithm of diffs in mergeEditor
	"mergeEditor.diffAlgorithm": "smart",

	// Count how many deletion are being made
	"mergeEditor.showDeletionMarkers": true
}